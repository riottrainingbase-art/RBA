export const metadata = { robots: { index: false, follow: false } };
import { PaymentComplete } from "@/components/payment-complete";
export default function Page(){return <PaymentComplete locale="ja"/>}